---
/**
 * Section 04 — The Companies.
 *
 * Identitas tiga perusahaan menjadi pusat section, baru disusul keterangan
 * singkat. Motion logo dibangun sebagai satu sequence di modules/brand-stages.
 */
import { companies } from '../../data/site';
---

<section class="section companies" id="companies">
  <header class="section-head companies-head">
    <h2 class="section-title h2-style" data-reveal>{companies.headline}</h2>
    <p class="section-intro" data-reveal data-reveal-stagger="0.05">{companies.intro}</p>
  </header>

  <ol class="company-grid">
    {
      companies.items.map((company) => (
        <li class="company-showcase" data-company-showcase>
          {/* `eager`, bukan `lazy`: ketiganya sudah di-preload di <head> (lihat
              Base.astro), dan `lazy` justru menahan pemakaian berkas yang sudah
              terlanjur diunduh sampai gridnya nyaris masuk layar — logonya baru
              muncul setelah animasi singkapnya jalan, jadi terlihat telat. */}
          <div class="company-logo-wrap" data-company-logo>
            <img src={company.logo} alt={company.name} loading="eager" fetchpriority="low" decoding="async" />
          </div>
        </li>
      ))
    }
  </ol>
</section>
